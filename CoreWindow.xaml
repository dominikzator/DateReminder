<Window x:Class="DateReminder.CoreWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        
    Title="CoreWindow" Height="200" Width="200" Visibility="Hidden" WindowStyle="None">

    <Window.Resources>
        <Style x:Key="{x:Static MenuItem.SeparatorStyleKey}" TargetType="Separator">
            <Setter Property="Background" Value="Red" />
        </Style>
    </Window.Resources>
    <tb:TaskbarIcon x:Name="myNotifyIcon"
                    ToolTipText="Fallback ToolTip for Windows xp"
                    IconSource="/Icons/BellIcon.ico"
                    MenuActivation="LeftOrRightClick"
                    PopupActivation="DoubleClick">
        <tb:TaskbarIcon.ContextMenu>
            <ContextMenu
      Background="White">
                <MenuItem x:Name="QuitMenuItem" Header="Quit" Margin="0,0,0,0" >
                    <MenuItem.Icon>
                        <Image Source="/cancel_icon_png.png" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </MenuItem.Icon>
                </MenuItem>
            </ContextMenu>
        </tb:TaskbarIcon.ContextMenu>


    </tb:TaskbarIcon>

</Window>
