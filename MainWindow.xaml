<Window x:Class="DateReminder.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DateReminder"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" Background="#FFF9B84F">
    <StackPanel Margin="10">
        <Button Content="New Reminder" Click="NewReminder_Click"/>
        <TextBox Margin="0 5 0 5" TextChanged="TextBox_TextChanged"/>
        <ListView x:Name="RemindersListView" SelectionChanged="ContactsListView_SelectionChanged" MaxHeight="450" Margin="0, 5, 0, 0" Background="White">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <local:ReminderControl Reminder="{Binding}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</Window>
